# Tasks: Lean-Core Workflow Plugins

**Input**: Design documents from `/specs/001-lean-core-plugins/`

**Prerequisites**: `plan.md`, `spec.md`, `research.md`, `data-model.md`, `contracts/`

**Tests and slice rule**: Test-first parity and fail-closed security tests are mandatory. Every
implementation PR derived from these tasks stays <=250 authored changed lines. If a logical task needs
more than one PR, its child issue must enumerate the bounded slices before code starts. `[umbrella]`
entries are coordination checkpoints for the exact deletion-only PRs listed in the task and are not
implementation PRs. `[maw-rs]` and `[maw-plugins]` name the owning repository. External artifact
acceptance always precedes creation of the fresh downstream cutover branch. Every maw-rs
PR slice runs an isolated quick gate and unpiped/non-PTY full gate before merge; every maw-plugins
PR runs its complete external CI/pin/capability gates. Exact evidence is recorded. Documentation,
ledger, governance, convergence, and deletion slices are not exempt from the maw-rs full gate.

## Phase 1: Setup and Governance

**Purpose**: Make the program reproducible and create reviewed execution boundaries.

- [ ] T001 [maw-rs] Check in Spec Kit 0.16.5 generated infrastructure and feature artifacts under `.specify/`, `.agents/skills/`, and `specs/001-lean-core-plugins/` in a docs-only PR for #963; report exact generated/copied versus authored line counts, request the one-time generated-scaffold/program-documentation exception, obtain explicit reviewer approval, and include no product code.
- [ ] T002 [maw-rs] Write the ownership/decision ADR in `docs/decisions/lean-core-workflow-plugins.md`, explicitly superseding #743/#758, #273/#282, and #174/#214 while preserving #910/#911 host boundaries.
- [ ] T003 [P] [maw-rs] Freeze the bring/split source-caller-fixture matrix in `specs/001-lean-core-plugins/inventories/bring-split.md` from current `origin/alpha`.
- [ ] T004 [P] [maw-rs] Freeze the team/companion source-caller-alias-fixture-host-action matrix in `specs/001-lean-core-plugins/inventories/team.md`, including `artifact` and assigning wave only to Codex.
- [ ] T005 [P] [maw-rs] Freeze the Codex provider/account/more/wave source-caller-fixture matrix in `specs/001-lean-core-plugins/inventories/codex.md`, separating vendor policy from generic wake/attach needs.
- [ ] T006 [maw-rs] Translate the reviewed task phases into bounded child issues and maintain their links/status in issue #963 and `specs/001-lean-core-plugins/issue-ledger.md`.

---

## Phase 2: Foundational Runtime, Native Adapter, and Artifact Contracts

**Purpose**: Land every generic, fail-closed prerequisite before any native product owner is removed.

**CRITICAL**: No guest source may consume a typed surface until its host registration, maw-cli adapter,
ABI fixture, external Rust SDK binding, and CI conformance row are merged.

### Manifest routing and result contracts

- [ ] T007 [maw-rs] Add RED manifest-declared verb-help/version tests in `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs` and `crates/maw-cli/tests/verb_help_flag_cli.rs`.
- [ ] T008 [maw-rs] Implement backward-compatible flag ownership in `crates/maw-plugin-manifest/src/core_impl/surface_parsers.rs`, `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`, and `crates/maw-plugin-manifest/src/core_impl/wasm_mvp_runtime.rs`.
- [ ] T009 [maw-rs] Add RED legacy-exclusive `cli.routes`, runnable-plugin collision, CLI-owned staged-shadow, immutable legacy `cli.command` eligibility, and hash-covered non-overlapping/default/alias intent-grammar/resource-plan/call-budget tests in `crates/maw-plugin-manifest/src/core_impl/surface_parsers.rs`, `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`, `crates/maw-cli/src/core_impl/dispatcher.rs`, and `crates/maw-cli/src/core_impl/plugin_ls_render.rs`; preserve an unchanged hash/path-bound legacy capability manifest, but give a changed/new/typed manifest without intents and unmatched/ambiguous argv zero typed-host grants.
- [ ] T010 [maw-rs] Implement the exact `cli.routes` plus bounded intent/argument-role/effective-capability/resource-plan/mandatory-action-budget model/parser and immutable legacy-eligibility table in `crates/maw-plugin-manifest/src/core_impl/surface_parsers.rs`, `crates/maw-plugin-manifest/src/core_impl/capability_parsers.rs`, and `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`; reject intent grants outside the artifact static set, unbounded counters, or copied legacy eligibility, and compute native-shadow state only from maw-cli's authoritative route set in `crates/maw-cli/src/core_impl/dispatcher.rs` and `crates/maw-cli/src/core_impl/plugin_ls_render.rs`.
- [ ] T011 [maw-rs] Add RED canonical/alias route and subcommand/default/invalid intent matching, unforgeable `invokedCommand`/`invokedIntent`, effective-capability intersection, reviewed argument roles, one-shot replay through cloned host handles, and same-team cross-intent/resource substitution tests in `crates/maw-cli/tests/plugin_multi_command_cli.rs` and `crates/maw-plugin-manifest/tests/wasm_host_context.rs`; include list/status -> terminate/write denial, More status -> maintenance denial, Wave status -> teardown/create denial, `--only`/`--members`/`--keep` target restrictions, lead/adopted eligibility, and unchanged legacy-capability compatibility versus new typed-capability-without-intent denial.
- [ ] T012 [maw-rs] Implement one canonical `match_route`/`match_intent` result, host-owned intent/resource context, per-invocation effective `CapabilitySet` intersection, and shared atomic action-budget consumption in `crates/maw-plugin-manifest/src/core_impl/capability_parsers.rs`, `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_prelude.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_dispatch_audit.rs`, and `crates/maw-cli/src/core_impl/dispatcher.rs`; every host call validates intent/bound resource and exhausts mutation authority before dispatch, while invalid argv retains only guest usage rendering.
- [ ] T013 [maw-rs] Add RED multi-route help/version/list/bind/JSON consumer tests in `crates/maw-cli/tests/verb_help_flag_cli.rs`, `crates/maw-cli/src/core_impl/plugin_ls_render.rs`, and `crates/maw-cli/src/core_impl/plugin_manifest_bind_host.rs`.
- [ ] T014 [maw-rs] Migrate help/version and plugin-list consumers to the canonical route API in `crates/maw-plugin-manifest/src/core_impl/wasm_mvp_runtime.rs` and `crates/maw-cli/src/core_impl/plugin_ls_render.rs`.
- [ ] T015 [maw-rs] Migrate bind/JSON consumers to the canonical route API in `crates/maw-cli/src/core_impl/plugin_manifest_bind_host.rs` and `crates/maw-cli/src/core_impl/plugins.rs`.
- [ ] T016 [maw-rs] Add RED optional CLI result `exitCode` tests in `crates/maw-plugin-manifest/tests/wasm_host_context.rs` and `crates/maw-cli/tests/plugin_exit_code_cli.rs`.
- [ ] T017 [maw-rs] Implement optional validated CLI `exitCode` in `crates/maw-plugin-manifest/src/core_impl/capability_parsers.rs`, `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`, and `crates/maw-cli/src/core_impl/dispatcher.rs`.

### Base guest ABI, SDK, registry, and artifact proof

- [ ] T018 [maw-rs] After T007-T017, in bounded slices first make canonical Rust `maw plugin build <package>` use `cargo build --locked`, accept a root ship manifest beside `Cargo.toml`, emit deterministic `dist/plugin.wasm` without rewriting committed bytes, and test/document exact dist-to-root promotion/pin verification in `crates/maw-cli/src/core_impl/plugin.rs`, its existing test module, and `docs/guides/adding-a-plugin-artifact.md`; then publish a minimal versioned guest ABI fixture for legacy invoke/context/result plus canonical route/optional intent identity in `crates/maw-plugin-manifest/tests/fixtures/guest_abi_base_v1.json` and pin it with `crates/maw-plugin-manifest/tests/wasm_host_abi_contract.rs`. It contains no new host function and establishes the client-first rule: additive guest/SDK bindings must execute against the preceding host fixture before a host requirement is enabled.
- [ ] T019 [maw-plugins] Vendor the exact T018 fixture with maw-rs source SHA/digest; add the exact pinned toolchain/wasm target in `rust-toolchain.toml`; and create the capability-free core of `crates/maw-guest-sdk/Cargo.toml`, `crates/maw-guest-sdk/Cargo.lock`, `crates/maw-guest-sdk/src/lib.rs`, and `crates/maw-guest-sdk/tests/abi.rs`. Expose `cdylib`/`rlib`, invoke/result/context helpers, and target-gated externs behind a fakeable `Host` trait. Add a dedicated executable fixture—not an SDK export—in `crates/maw-guest-sdk/tests/fixtures/base-guest/Cargo.toml`, `crates/maw-guest-sdk/tests/fixtures/base-guest/Cargo.lock`, `crates/maw-guest-sdk/tests/fixtures/base-guest/src/lib.rs`, and `crates/maw-guest-sdk/tests/fixtures/base-guest/plugin.json`.
- [ ] T020 [maw-plugins] Add the base SDK/fixture native fake-Host tests and canonical T018 `maw plugin build` invocation to `.github/workflows/ci.yml`; require every new Rust guest manifest to expose `cdylib` and `rlib`, commit its Cargo.lock, and use the pinned complete Rust/wasm/build environment. Build the base guest twice from clean target/dist outputs, require both `dist/plugin.wasm` byte streams to equal the committed root artifact/pin, and execute that artifact. Add reusable exact-host compatibility execution in `.github/workflows/maw-rs-compat.yml` and `scripts/run-maw-rs-compat.ts`: it checks out a caller-supplied immutable maw-rs SHA, accepts only a registry-resolved package name, verifies the committed artifact pin, and runs without arbitrary ref/package shell interpolation.
- [ ] T021 [maw-plugins] In a bounded slice, update `scripts/gen-registry.ts`, add `scripts/gen-registry.test.ts`, and wire it through `package.json` so mutually exclusive `cli.routes` emits every canonical route deterministically to one immutable package/pin and refuses route/alias collisions; preserve legacy `cli.command` bytes.
- [ ] T022 [maw-plugins] Add an automated forbidden-capability/intent scan in `scripts/check-capability-policy.ts`, one negative case per category plus all-new-scoped-manifest enumeration in `scripts/check-capability-policy.test.ts`, a test script in `package.json`, and execution in `.github/workflows/ci.yml`; cover wildcard/raw process, environment, filesystem, tmux, trust, secret, arbitrary CLI, intent grant outside static set, overlapping/default intent, unbounded/replayed action budget, and unbound effectful resource. Preserve only hash/path-bound pre-#963 `cli.command` static semantics with no new typed grant; separately permit the exact current `packages/team/plugin.json` broad grants as one temporary named exemption expiring at T126. Prove neither eligibility nor exemption can be copied to another package/hash, and make drift fail closed.
- [ ] T023 [maw-rs] Restore committed external-artifact host invocation and one-startup call-count coverage in `crates/maw-cli/tests/plugin_artifact_parity.rs`; add the immutable external source/artifact lock `crates/maw-cli/tests/fixtures/external-plugin-artifacts.lock.json` and fail-closed `scripts/plugin-test-fixtures.sh`, and wire the helper through `scripts/gate.sh` plus every affected `.github/workflows/ci.yml` cargo dimension. The lock has one monotonically advancing maw-plugins `main` tree SHA plus preserved per-package source/artifact hashes; an update must be a descendant and cannot downgrade or remove an accepted row. The helper fetches/caches only that tree under gate-owned `TMPDIR`, verifies every package/artifact SHA, exports the verified fixture directory, and treats missing network/cache/hash/downgrade/removal as failure—never skip, ambient ghq lookup, or product-artifact vendoring.
- [ ] T024 [maw-rs] Extend known external ownership/aliases and help/doctor truth in `crates/maw-cli/src/core_impl/plugin_known_verbs.rs`, `crates/maw-cli/src/core_impl/completions.rs`, `crates/maw-cli/src/core_impl/plugin_ls_render.rs`, and `crates/maw-cli/src/core_impl/doctor.rs`, with exact convergence/repair tests in `crates/maw-cli/tests/lean_core_ownership.rs`.

**Checkpoint**: Capability-free Rust guests such as bring can be accepted now; no typed host surface is implied.

### Injected native workflow adapter

- [ ] T025 [maw-rs] Add RED unavailable/fake/injected `WorkflowHostOps` construction and dependency-direction tests in `crates/maw-plugin-manifest/tests/wasm_host_context.rs` and `crates/maw-cli/tests/plugin_workflow_host.rs`.
- [ ] T026 [maw-rs] Define the thread-safe `WorkflowHostOps` trait/default and injection plumbing in `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_prelude.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_setup.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/runtime_extism.rs`, and `crates/maw-cli/src/core_impl/dispatcher.rs`; add `crates/maw-cli/src/core_impl/plugin_workflow_host.rs` as the production adapter shell without product policy.
- [ ] T027 [maw-rs] Add RED generic planLaunch/ensure-session/launch/resume/finish/terminate/keep-session-guard/wait-reinventory DTO, capability, 30-second bound, host-rendered non-authority displayCommand, cycle, ambiguous-outcome, opaque `LifecycleOutcomeRef` replay/substitution, exact `ContentRef` prompt binding, Team-spawn parent/session refs, boundWorkspaceRef versus digest-bound `nativeDefaultResolution` selection, durable-registry-member workspace issuance, no-invented-repo behavior from unrelated cwd, stale/unissued command/content receipts, and forged/stale/cross-team/cross-member/cross-pane `WorkspaceRef`/`EngineRef`/`LifecycleMemberRef` tests in `crates/maw-plugin-manifest/tests/wasm_host_security/workflow_lifecycle.rs`, wired in `crates/maw-plugin-manifest/tests/wasm_host_security.rs` in the same slice; prove launch-only More authority cannot terminate and destructive actions re-inventory the bound pane.
- [ ] T028 [maw-rs] Implement and register ref-only `maw.workflow.lifecycle.v1` with non-mutating planLaunch/displayCommand plus `LifecycleOutcomeRef` through `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_workflow_lifecycle.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_prelude.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_setup.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_dispatch_audit.rs`, `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`, and `crates/maw-plugin-manifest/src/core_impl/capability_parsers.rs`; requests accept only issued bound-workspace/native-default-resolution selections, engine, lifecycle-member, exact prompt ContentRef, and reviewed Team-spawn session refs.
- [ ] T029 [maw-rs] Implement native wake/done/session guard/bounded wait-and-reinventory lifecycle adapter methods in `crates/maw-cli/src/core_impl/plugin_workflow_host.rs` and inject them from `crates/maw-cli/src/core_impl/dispatcher.rs`; validate and materialize issued workspace/native-default-resolution/registry-member, engine, member, and exact ContentRef prompt bindings against durable authority records, omit repo overrides for native-default resolution, re-inventory owned panes before destructive actions, preserve launch-only scope, and keep native wake/attach entrypoints.
- [ ] T030 [maw-rs] Add RED pane-membership, preflight-all, one-shot replay, and opaque `PanePayloadRef` operatorArg/enterOnly/confirmedMissionPointer tests in `crates/maw-plugin-manifest/tests/wasm_host_security/workflow_pane_submit.rs`; cover invented/substituted/cross-role text, mission pointer before confirmed write, partial, ambiguous, cloned-handle replay, and retry denial, and wire the fragment in `crates/maw-plugin-manifest/tests/wasm_host_security.rs` in the same slice.
- [ ] T031 [maw-rs] Implement and register `PanePayloadRef`-only `maw.workflow.pane_submit.v1` through `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_workflow_pane_submit.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_prelude.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_dispatch_audit.rs`, `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`, and `crates/maw-plugin-manifest/src/core_impl/capability_parsers.rs`.
- [ ] T032 [maw-rs] Implement native pane payload issuance, all-target preflight/submission, and confirmed-mission-write binding in `crates/maw-cli/src/core_impl/plugin_workflow_host.rs` and inject them from `crates/maw-cli/src/core_impl/dispatcher.rs` without changing low-level raw key semantics.

### Tmux, roots, context, and consent

- [ ] T033 [maw-rs] Add RED split-only schema/capability/argv/no-mutation plus full reviewed target/orientation/percentage/optional-command `SplitPlanRef` tests in `crates/maw-plugin-manifest/tests/wasm_host_security/tmux_split.rs`; cover absent-command addition, command/target/size/direction substitution, cross-role/intent, forged/stale, and cloned-handle replay, and wire the fragment in `crates/maw-plugin-manifest/tests/wasm_host_security.rs` in the same slice.
- [ ] T034 [maw-rs] Implement and register one-shot `SplitPlanRef`-only `maw.tmux.split.v1` in `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_tmux_split.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_prelude.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_dispatch_audit.rs`, `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`, and `crates/maw-plugin-manifest/src/core_impl/capability_parsers.rs`; implement/inject the only production tmux split adapter in `crates/maw-cli/src/core_impl/plugin_workflow_host.rs` and `crates/maw-cli/src/core_impl/dispatcher.rs`, with no raw tmux execution in the leaf host.
- [ ] T035 [maw-rs] Add separate RED suites for bounded `PaneInventoryScopeRef` inventory fields/issued pane+cwdRef and manifest-declared no-text pane observations in `crates/maw-plugin-manifest/tests/wasm_host_security/tmux_pane_inventory.rs` and `crates/maw-plugin-manifest/tests/wasm_host_security/tmux_pane_observe.rs`; cover Team/session/Wave/More scope rows, cross-team/session metadata/ref denial, title/index facts, host-error-not-empty/false, marker/read-budget bounds, cloned-handle exhaustion, and unissued/cross-pane/cross-scope refs, and wire each fragment in `crates/maw-plugin-manifest/tests/wasm_host_security.rs` in its same slice.
- [ ] T036 [maw-rs] In separate <=250-line slices, implement and register `PaneInventoryScopeRef`-only `maw.tmux.pane_inventory.v1` with issued cwdRefs and scope-bound manifest-hash-covered `maw.tmux.pane_observe.v1` descriptor/schema through `crates/maw-plugin-manifest/src/core_impl/surface_parsers.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_tmux_pane_inventory.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_tmux_pane_observe.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_prelude.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_dispatch_audit.rs`, `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`, and `crates/maw-plugin-manifest/src/core_impl/capability_parsers.rs`, sharing per-invocation authority/budgets. Implement/inject bounded production tmux inventory/capture/classification adapters in `crates/maw-cli/src/core_impl/plugin_workflow_host.rs` and `crates/maw-cli/src/core_impl/dispatcher.rs`; no raw tmux format/capture path lives in the leaf host or guest.
- [ ] T037 [maw-rs] Add RED root/relative DTO, construction-is-resolve-only/no-filesystem-write, listRoot-versus-root-mutation, intent-bound resolve-only `previewDisplayPath`, traversal/symlink/precedence/merged-fallback/non-authority-displayPath and closed document-selector tests for `tool-teams`, `team-vault`, `team-state`, `artifact-store`, and `mission-store` in `crates/maw-plugin-manifest/tests/wasm_host_security/manifest_roots.rs` and `crates/maw-plugin-manifest/tests/wasm_host_security/operator_input.rs`; cover approved route/argument-role absolute paths, same bytes in an unapproved slot, implicit/single charters, nonexistent preview targets, issued workspace/mission-store refs, wrong route/team/repository, and wire both fragments in `crates/maw-plugin-manifest/tests/wasm_host_security.rs` in the same slice. Freeze ordinary named-root, operator-document, and archive-walker symlink behavior and record one explicit filesystem-symlink human approval before implementation.
- [ ] T038 [maw-rs] In bounded slices, implement resolve-only `NamedRoot { primary, readFallbacks }`, exact-capability listRoot and manifest-intent-bound no-create/no-ref `previewDisplayPath`, versioned root/relative requests, merged read/list, non-authority displayPath, closed-selector `maw.input.document.v1`, document-issued workspace refs, and repo-identity-to-mission-store-ref validation through `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_fn_args.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_prelude.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/exec_path_helpers.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_roots_state.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_setup.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_fs_read_write.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_fs_mkdir_remove_list.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_dispatch_audit.rs`, `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`, and `crates/maw-plugin-manifest/src/core_impl/capability_parsers.rs`; expose only DTO/validation/dispatch in the leaf crate. Implement and inject all production root resolution, document/read/list filesystem I/O in `crates/maw-cli/src/core_impl/plugin_workflow_host.rs` and `crates/maw-cli/src/core_impl/dispatcher.rs`; create parents only within a validated mutation operation.
- [ ] T039 [maw-rs] Add RED atomic-private-write mode/flush/rename/unknown-field and bounded operation/preview `displayPath`, receipt-bound document replace/archive-copy forged/stale/path-swap/no-mutation, named-root archiveCopy/mergeCopy, durable-complete-copy-before-remove, partial-copy-source-untouched, later tracked-write remove receipt, generic authority-projection change refusal, persist-only explicit `WorkspaceValueRef` storage/no-existence plus never-lifecycle/Git/later-validation cases, `ContentRef` source/template/semantic-role substitution, `LifecycleOutcomeRef`/`BatchOutcomeRef`/peer-decision replay/reorder/substitution, cloned-handle action-budget replay, typed authority-write pending/state/final-record partial outcomes and reconciliation, locked per-domain pre-epoch native snapshot, unsealed/post-epoch missing-record refusal, guest-write-then-reread ContentRef/launch/message laundering, forged archived command or pre-epoch prompt, and guest-crafted wave-state cross-member/worktree/branch cases in `crates/maw-plugin-manifest/tests/wasm_host_security/filesystem_protection.rs`, wired in `crates/maw-plugin-manifest/tests/wasm_host_security.rs` in the same slice. Freeze native ordinary named-root/operator-document/archive-walker symlink follow/skip behavior and include every affected row in the T037 filesystem-symlink human gate before implementation.
- [ ] T040 [maw-rs] In bounded slices, implement and register `maw.state.authority_write.v1` with one-shot closed generic `createFromReceipts`, `upsertMemberFromRefs`, `removeBoundMember`, `claimLeadFromCurrentSession`, `writeBoundContent`, `recordLifecycleOutcome`, `recordBatchOutcome`, `recordPeerDecision`, and `retireObject` DTO actions; primary-only named-root create/append/atomic replace with immutable authority projection; receipt-bound document `atomicReplace`/`archiveCopy`; named-root `archiveCopy`/`mergeCopy`/`removeAfterArchive`; and durable-copy-first `archive_then_remove` in `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_fs_read_write.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_fs_mkdir_remove_list.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_roots_state.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_authority_provenance.rs`, and the registration/capability paths named in T038. Peer writes consume exact `PeerTrustRef`/`ConsentDecisionRef`; mission writes issue only a matching confirmed-mission `PanePayloadRef`; lead claim consumes host current-session/team/source generation only. Implement guest-inaccessible cross-process per-domain locks, retained dispatcher transition hooks, writer-generation checks, durable record/reconciliation, and isolated fake-domain seeding in `crates/maw-cli/src/core_impl/plugin_workflow_authority.rs`, `crates/maw-cli/src/core_impl/plugin_workflow_host.rs`, and `crates/maw-cli/src/core_impl/dispatcher.rs`; add native `maw plugin authority reimport <domain> <object> [<member>] --expect-sha256 <digest> [--yes]` parser/help/confirmation/redacted-audit behavior in `crates/maw-cli/src/core_impl/plugin.rs`, `crates/maw-cli/src/core_impl/completions.rs`, and `crates/maw-cli/tests/plugin_authority_reimport.rs`. All production create/append/replace/archive/copy/remove and authority-record filesystem I/O is implemented through the injected adapter in `crates/maw-cli/src/core_impl/plugin_workflow_host.rs`; leaf host files own DTO/validation/dispatch only. Native code materializes authority fields from receipts/outcomes, including persist-only operatorLiteralPending WorkspaceValueRef without minting WorkspaceRef; partial state/record commit is ambiguous, unsealed-domain writes and post-epoch missing records fail closed, and recovery accepts no raw path or guest call.
- [ ] T041 [maw-rs] Add RED safe semantic team/session/original-argv context, one fakeable invocation `nowMillis`, denied ambient-environment/clock, bound operator/implicit/single-team documents, and opaque `ContentRef`/`SplitPlanRef`/`PanePayloadRef`/`PaneInventoryScopeRef`/`WorkspaceRef`/`WorkspaceValueRef`/`GitRefRef`/`EngineRef`/`LifecycleMemberRef`/`LifecycleOutcomeRef`/`BatchOutcomeRef`/`IssueRef`/`PeerTrustRef`/`ConsentDecisionRef`/Team+Swarm session-env issuance tests in `crates/maw-plugin-manifest/tests/wasm_host_context.rs`; cover wrong argument/content role, charter legacy model fallback, selected-pane local-adopt engine classification, cross-pane/member, forged, stale-digest, missing-provider, unissued/cross-intent/cross-outcome refs, generic guest-written authority fields, per-domain pre-epoch snapshot, post-epoch missing/pending/mismatched durable records, and explicit native re-import for orphan authority.
- [ ] T042 [maw-rs] Implement reviewed semantic context, one sampled invocation clock, shared finite/one-shot action budgets, and invocation-local document/value/content/split-plan/pane-payload/pane-inventory-scope/layout-pane-set/workspace/workspace-value/git-ref/engine/lifecycle-member/lifecycle-outcome/batch-outcome/issue/peer-trust/consent-decision/Team+Swarm-session receipts in `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_prelude.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_setup.rs`, `crates/maw-plugin-manifest/src/core_impl/capability_parsers.rs`, `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`, `crates/maw-cli/src/core_impl/dispatcher.rs`, and `crates/maw-cli/src/core_impl/plugin_workflow_authority.rs`; named-state-derived authority requires an exact final durable record plus live revalidation, and only the locked pre-guest domain epoch may inventory legacy arbitrary commands. Preserve `MAW_RS_TEAM_PSI` only as native root-selection input.
- [ ] T043 [maw-rs] Add RED typed consent accepted/pending/denied/unreachable/invalid with host-owned per-status display fields, local-pending-persist-before-peer-POST ordering, unreachable-after-persist request-id-only recovery, pending request-id/one-time-code/expiry/peer, trust-mutation denial, and named-peer public-descriptor trusted/untrusted/missing/unknown tests in `crates/maw-plugin-manifest/tests/wasm_host_security/config_consent.rs`; include opaque route/intent/argument-bound `PeerTrustRef`/`ConsentDecisionRef`, forged fact/status/cross-peer/subcommand/stale/replay and skip-consent refusal, and wire the fragment in `crates/maw-plugin-manifest/tests/wasm_host_security.rs` in the same slice.
- [ ] T044 [maw-rs] In bounded slices, implement and register one-shot `maw.consent.request.v1` and read-only `maw.peer.trust_status.v1` with opaque accepted/trusted decision refs through `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_config_consent.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_prelude.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_dispatch_audit.rs`, `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`, and `crates/maw-plugin-manifest/src/core_impl/capability_parsers.rs`.
- [ ] T045 [maw-rs] Implement native consent and peer-resolution/trust adapters plus immediate pre-commit decision revalidation in `crates/maw-cli/src/core_impl/plugin_workflow_host.rs` and inject them from `crates/maw-cli/src/core_impl/dispatcher.rs`, keeping human auth/trust decisions and secret configuration native.

### Companion and worktree primitives

- [ ] T046 [maw-rs] Add RED ordered join/break/layout validation immediately before each bound action, truthful partial-outcome after a later invalid target, current-pane plus charter/member/intent-bound `LayoutPaneSetRef`, broad-inventory/cross-team/cross-intent substitution, and syntactically-valid-unissued-pane refusal tests in `crates/maw-plugin-manifest/tests/wasm_host_security/tmux_layout.rs`, wired in the root `crates/maw-plugin-manifest/tests/wasm_host_security.rs` harness in the same slice.
- [ ] T047 [maw-rs] Implement and register `LayoutPaneSetRef`-only `maw.tmux.layout.v1` plus shared per-invocation intent/resource authority through `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_tmux_layout.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_prelude.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_setup.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_dispatch_audit.rs`, `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`, and `crates/maw-plugin-manifest/src/core_impl/capability_parsers.rs`; preserve native sequential validation/partial ordering rather than adding an all-plan atomicity correction.
- [ ] T048 [maw-rs] Implement current-pane issuance and join/break/layout native adapter methods in `crates/maw-cli/src/core_impl/plugin_workflow_host.rs` with exact pane-id/layout validation.
- [ ] T049 [maw-rs] Add RED ten-pane bound, created-pane-id, opaque `BatchOutcomeRef` replay/reorder/cross-result/pane substitution, route/argument-role-issued custom-executable `EngineRef`, accepted-provider/native-built-in `EngineRef` default, no-arg exact three-Claude swarm, `--parent`/`--parent-session-id`/`--session-id` refs, exact one-member session-env rule, arbitrary-env/control/literal-executable/forged-ref refusal, layout/title, partial, and shell-injection tests in `crates/maw-plugin-manifest/tests/wasm_host_security/workflow_batch_launch.rs`, wired in the root `crates/maw-plugin-manifest/tests/wasm_host_security.rs` harness in the same slice.
- [ ] T050 [maw-rs] Implement and register `EngineRef`/`SessionEnvironmentRef`-only `maw.workflow.batch_launch.v1` with opaque `BatchOutcomeRef` through `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_workflow_batch_launch.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_prelude.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_dispatch_audit.rs`, `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`, and `crates/maw-plugin-manifest/src/core_impl/capability_parsers.rs`.
- [ ] T051 [maw-rs] Implement split/current-window/layout/title launch adapter methods in `crates/maw-cli/src/core_impl/plugin_workflow_host.rs`; allow defaults only through accepted-provider/native-built-in EngineRefs, issue custom EngineRefs only from reviewed original Swarm agent positionals, and materialize only the two exact swarm session variables from issued refs under the one-member rule.
- [ ] T052 [maw-rs] Add separate RED suites for non-mutating worktree plan-create, create/inspect/marker/prune/archive/remove/delete-branch, durable repo-identity/mission-store resolution, and current-repo issue-context fetch in `crates/maw-plugin-manifest/tests/wasm_host_security/worktree_manage.rs` and `crates/maw-plugin-manifest/tests/wasm_host_security/repo_issue_context.rs`; cover host-issued `GitRefRef`/`IssueRef`, unissued/substituted/cross-repo/replayed ref refusal, branch/marker facts, marker-write partial outcomes, planned/existing workspace, cwd, engine, mission, and wave-state-owned member refs, plan/create race and collision, legacy mission-path migration, forged/stale/cross-pane/member/team/branch/repository refs, notRepo, repo/ref/path validation, external-content wrapping, read limits/budgets, and ambiguous outcomes, and wire each fragment in `crates/maw-plugin-manifest/tests/wasm_host_security.rs` in the same slice.
- [ ] T053 [maw-rs] In separate <=250-line slices, implement and register host-issued `GitRefRef` + `plannedWorkspaceRef` plan/create plus issued-`WorkspaceRef`/`EngineRef`/`LifecycleMemberRef`-only `maw.worktree.manage.v1`, including digest-bound wave-state issuance/re-read and derived workspace/branch authority, durable repo-identity/mission-store-ref resolution, and one-shot `IssueRef`-only `maw.repo.issue_context.v1` through `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_worktree_manage.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_repo_issue_context.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_prelude.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_dispatch_audit.rs`, `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`, and `crates/maw-plugin-manifest/src/core_impl/capability_parsers.rs`.
- [ ] T054 [maw-rs] Implement validated native `GitRefRef`/worktree planning, repo-identity/mission-store, and original-argument/current-repository `IssueRef` issuance/fetch adapter methods in `crates/maw-cli/src/core_impl/plugin_workflow_host.rs` using existing worktree/GitHub boundaries and durable authority records, never guest-supplied or merely guest-persisted raw Git/gh argv, paths, branches, issue numbers, or credentials.

### Provider, health, maintenance, and occupancy

- [ ] T055 [maw-rs] Add RED separate provider-id versus executable-id, descriptor/export/operation/environment-value-schema, bounded issued-root candidate, and `InvokeSource::Provider` parse tests in `crates/maw-plugin-manifest/src/core_impl/surface_parsers.rs` and `crates/maw-plugin-manifest/tests/provider_descriptor.rs`, including provider/executable mismatch plus absolute/traversal/stale/unissued provider-root rejection.
- [ ] T056 [maw-rs] Implement the provider descriptor, opaque candidate-fact request, and plan response DTO in `crates/maw-plugin-manifest/src/core_impl/surface_parsers.rs`, `crates/maw-plugin-manifest/src/core_impl/capability_parsers.rs`, and `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`.
- [ ] T057 [maw-rs] Add RED fresh-instance host-to-guest provider export and shared typed recursion-stack tests in `crates/maw-plugin-manifest/tests/wasm_host_security/engine_provider.rs`; derive the Provider-mode table from complete `HOST_FN_NAMES`/the ABI fixture and prove every registered host import is a refusal stub with an empty allowlist, then wire the fragment in the root `crates/maw-plugin-manifest/tests/wasm_host_security.rs` harness in the same slice.
- [ ] T058 [maw-rs] Implement the dedicated plan-only provider-host constructor, refusal imports, shared stack, export invocation, and recursion denial in `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_prelude.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_setup.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_dispatch_audit.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/runtime_extism.rs`, `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`, and `crates/maw-cli/src/core_impl/plugin_workflow_host.rs`.
- [ ] T059 [maw-rs] Add RED provider-id/executable-id mismatch, argv/env-value-schema/unissued-root/missing/refused/no-fallback, absent-provider actionable package/source repair, and native composition tests for CLI prompt, directory `wake.prompt`, `commands.<engine>`, higher-priority `commands.<engine>-resume`, charter engine-command/resume, empty provider response in operator-complete mode, and exactly-once prompt append in `crates/maw-cli/tests/engine_provider_contract.rs`.
- [ ] T060 [maw-rs] Implement provider selection/plan validation helpers in `crates/maw-cli/src/core_impl/plugin_workflow_host.rs` plus the minimal provider-id/executable/package/source repair catalog in `crates/maw-cli/src/core_impl/plugin_provider_catalog.rs`, without yet replacing native wake policy or retaining vendor launch/profile command policy.
- [ ] T061 [maw-rs] Add RED issued-`EngineRef`/`WorkspaceRef` provider availability, executable/config-command status, auth/trust/isolation, reviewed display-fact, selected-pane local-adopt classification with exact claude/codex/omx matches plus frozen live-nonshell default-to-Claude, cross-pane/stale refusal, and raw-secret/config-byte/current-command non-disclosure tests in `crates/maw-plugin-manifest/tests/wasm_host_security/engine_health.rs`, wired in `crates/maw-plugin-manifest/tests/wasm_host_security.rs` in the same slice.
- [ ] T062 [maw-rs] Implement and register ref-only `maw.engine.health.v1` through `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_engine_health.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_prelude.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_dispatch_audit.rs`, `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`, and `crates/maw-plugin-manifest/src/core_impl/capability_parsers.rs`.
- [ ] T063 [maw-rs] Implement secret-reading/non-secret-fact health adapter methods and selected-pane conservative engine classification into accepted-provider/frozen-built-in EngineRefs in `crates/maw-cli/src/core_impl/plugin_workflow_host.rs` with injected platform/config fixtures, including merged command resolution/status, `.maw-engine` validation, and only frozen non-authority display fields.
- [ ] T064 [maw-rs] Add RED descriptor-bound maintenance operation/program/argv/no-reentry tests in `crates/maw-plugin-manifest/tests/wasm_host_security/engine_maintenance.rs`, wired in the root `crates/maw-plugin-manifest/tests/wasm_host_security.rs` harness in the same slice.
- [ ] T065 [maw-rs] Implement and register `maw.engine.maintenance.v1` through `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_engine_maintenance.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_dispatch_audit.rs`, `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`, and `crates/maw-plugin-manifest/src/core_impl/capability_parsers.rs`.
- [ ] T066 [maw-rs] Implement executable-bound maintenance adapter methods in `crates/maw-cli/src/core_impl/plugin_workflow_host.rs`; provider output cannot select a program.
- [ ] T067 [maw-rs] Add RED occupancy-descriptor validation, secret marker, wildcard/path, finite read budget, raw-value non-disclosure, and non-authority pid/displayHome parity-field tests in `crates/maw-plugin-manifest/tests/wasm_host_security/account_occupancy.rs`, wired in `crates/maw-plugin-manifest/tests/wasm_host_security.rs` in the same slice; freeze native proc/ps failure -> `free`/`--free` behavior and obtain explicit human approval before changing it to literal `unknown` excluded from `--free`.
- [ ] T068 [maw-rs] Implement the manifest-hash-covered occupancy descriptor and register `maw.account.occupancy.v1` through `crates/maw-plugin-manifest/src/core_impl/surface_parsers.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_account_occupancy.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host.rs`, `crates/maw-plugin-manifest/src/core_impl/wasm_host/host_dispatch_audit.rs`, `crates/maw-plugin-manifest/src/core_impl/discovery_runtime.rs`, and `crates/maw-plugin-manifest/src/core_impl/capability_parsers.rs`.
- [ ] T069 [maw-rs] Implement finite-budget injected Linux/macOS occupancy probe adapter methods in `crates/maw-cli/src/core_impl/plugin_workflow_host.rs`, returning opaque facts only and, only after T067 approval, mapping probe uncertainty to `unknown` rather than availability.

### Complete typed ABI and SDK extension

- [ ] T070 [maw-rs] Publish the complete versioned typed-host ABI fixture and registration-conformance test in `crates/maw-plugin-manifest/tests/fixtures/guest_abi_v1.json` and `crates/maw-plugin-manifest/tests/wasm_host_abi_contract.rs`, covering `HOST_FN_NAMES`, include/dispatch registration, DTO versions, route/intent/effective-capability/resource-plan plus finite/one-shot action-budget context, Provider denial equal to the complete host-function set, and every Phase-2 surface including listRoot/previewDisplayPath/displayPath/mission-store/archive-copy, all closed `maw.state.authority_write.v1` actions and durable-record/lifecycle-outcome/batch-outcome responses, document/`ContentRef`/`SplitPlanRef`/`PanePayloadRef`/`PaneInventoryScopeRef`/layout-pane-set/workspace/workspace-value/cwd/`GitRefRef`/engine/lifecycle-member/`IssueRef`/`PeerTrustRef`/`ConsentDecisionRef`/Team+Swarm-session-environment receipts, pane observation, peer trust, repo issue context, worktree marker facts, and issued authority. Preserve client-first evolution: prove every additive SDK binding against the immediately preceding host fixture before this host fixture makes it mandatory.
- [ ] T071 [maw-plugins] After T070 merges, vendor the complete typed fixture with maw-rs source SHA/digest and extend `crates/maw-guest-sdk/src/lib.rs` plus `crates/maw-guest-sdk/tests/abi.rs` in bounded per-surface slices; preserve the T019 capability-free API while adding fakeable typed Host methods for every listed ref, finite/one-shot budget and closed semantic authority-write action/outcome, only after each native surface is accepted.
- [ ] T072 [maw-plugins] Extend `.github/workflows/ci.yml` so every typed SDK binding and every new Rust guest runs native fake-Host tests plus its wasm build against the vendored complete fixture; retain the T020 base loop and add route-only combined-Codex registry generation coverage.

**Checkpoint**: The complete native adapter and extended external SDK can host bounded typed-workflow guests; capability-free bring may already have moved.

---

## Phase 3: User Story 1 - Native Kernel Stays Stable (Priority: P1)

**Goal**: Establish a non-negotiable regression baseline used by every cutover.

- [ ] T073 [P] [maw-rs] [US1] Freeze wake plus `hey`/`peek`/`serve` native-kernel regression rows and source boundaries in `crates/maw-cli/src/core_impl/wake_tests.rs`, `crates/maw-cli/src/core_impl/send_federation.rs`, `crates/maw-cli/src/core_impl/tmux_peek.rs`, `crates/maw-cli/src/core_impl/serve_tests.rs`, and `crates/maw-cli/tests/native_send_wake_http.rs` without changing production; retain the exact accepted `https://god.buildwithoracle.com` serve-origin rows.
- [ ] T074 [P] [maw-rs] [US1] Freeze attach dispatcher, sleeping-target wake, and binary fast-path rows in `crates/maw-cli/src/core_impl/attach_private_tests.rs`, `crates/maw-cli/tests/attach_alias_cli.rs`, and `crates/maw-cli/tests/native_attach_view_stream_split.rs`.
- [ ] T075 [maw-rs] [US1] Add `crates/maw-cli/tests/lean_core_native_boundary.rs` asserting wake/attach/a/hey/peek/serve remain native and scoped optional verbs do not.
- [ ] T076 [maw-rs] [US1] Define exact focused wake/attach/hey/peek/serve twice-green commands and evidence fields in `specs/001-lean-core-plugins/quickstart.md` and `specs/001-lean-core-plugins/issue-ledger.md`; until #964 lands, every slice touching `crates/maw-cli/src/core_impl/*.rs` also runs and records explicit standalone rustfmt checks over the touched fragments.

---

## Phase 4: User Story 2 - Bring Plugin MVP (Priority: P1)

- [ ] T077 [P] [maw-plugins] [US2] Add RED raw-argv/text/JSON/help/error parity tests in `packages/bring/tests/bring_contract.rs` from frozen maw-rs bring suites.
- [ ] T078 [maw-plugins] [US2] Create the capability-free Rust WASM guest in `packages/bring/Cargo.toml`, `packages/bring/Cargo.lock`, and `packages/bring/src/lib.rs`, preserving plan-only quirks.
- [ ] T079 [maw-plugins] [US2] Add `packages/bring/plugin.json`, build/pin `packages/bring/plugin.wasm`, update `registry.json`, and require two clean canonical T018 builds to be byte-identical to each other and that committed pin. Pass T020/T022, directly invoke the committed bytes through the exact-host compatibility workflow against the frozen maw-rs SHA, merge to `main`, and record accepted source/artifact/run SHAs; it does not wait for unrelated typed-host ABI T025-T072.
- [ ] T080 [maw-rs] [US2] Only after T079, on the fresh post-acceptance branch, pin the accepted Bring commit/artifact in `crates/maw-cli/tests/fixtures/external-plugin-artifacts.lock.json`, add downstream RED alias/missing/refusal/help/completion cases, and convert every retained native bring integration row to the deterministic fail-closed bridge from T023 in `crates/maw-cli/tests/plugin_missing_verb_cli.rs`, `crates/maw-cli/tests/verb_help_flag_cli.rs`, `crates/maw-cli/tests/bring_cli.rs`, `crates/maw-cli/tests/auth_discover_worktree_ls_tail_edges.rs`, `crates/maw-cli/tests/ls_bring_remaining_edges.rs`, `crates/maw-cli/tests/worktree_ls_bring_cli_edges.rs`, `crates/maw-cli/tests/plugin_pair_route_render_edges.rs`, and `crates/maw-cli/src/core_impl/completions.rs`; preserve existing fixtures rather than deleting them and require the paired external committed-byte compatibility run against this exact downstream SHA before merge.
- [ ] T081 [maw-rs] [US2] On a fresh post-T079 branch, atomically remove bring/b `DISPATCH_304` from `crates/maw-cli/src/core_impl/session_list_plan.rs`, activate ownership in `crates/maw-cli/src/core_impl/plugin_known_verbs.rs`, `crates/maw-cli/src/core_impl/help_meta.rs`, and `crates/maw-cli/src/core_impl/completions.rs`, and pass artifact/missing/refusal plus wake/attach/full gate before merge.
- [ ] T082 [maw-rs] [US2] Delete only inert bring render/import/dependency bytes from `crates/maw-cli/src/core_impl/session_list_plan.rs`, `crates/maw-cli/src/core_impl/dispatcher.rs`, `crates/maw-cli/Cargo.toml`, and `Cargo.lock`, preserving shared JSON helpers and gating before merge.
- [ ] T083 [maw-rs] [US2] Record committed-artifact parity, missing/refusal, wake/attach, exact gates, source/artifact/diff hashes, and merged SHAs in `specs/001-lean-core-plugins/issue-ledger.md` and the bring child issue.

---

## Phase 5: User Story 3 - Split Plugin (Priority: P1)

- [ ] T084 [P] [maw-plugins] [US3] Add RED split parser/argv/dry-run/security cases in `packages/split/tests/split_contract.rs`, including first-arg `-v`, equals flags, controls, pct bounds, and exact full operator-argument-to-`SplitPlanRef` binding.
- [ ] T085 [maw-plugins] [US3] Create the Rust WASM split guest in `packages/split/Cargo.toml`, `packages/split/Cargo.lock`, and `packages/split/src/lib.rs` using only one-shot `SplitPlanRef` execution through `maw.tmux.split.v1`.
- [ ] T086 [maw-plugins] [US3] Add the exact Split invocation-intent/argument-role/action-budget table to `packages/split/plugin.json`, build/pin `packages/split/plugin.wasm`, update `registry.json`, pass T020/T072/T022 including two clean canonical T018 rebuilds equal to the committed bytes, invoke those bytes through the exact-host compatibility workflow against the frozen maw-rs SHA, pass CI, merge to `main`, and record accepted source/artifact/run SHAs.
- [ ] T087 [maw-rs] [US3] Only after T086, on the fresh post-acceptance branch, pin the accepted Split commit/artifact in `crates/maw-cli/tests/fixtures/external-plugin-artifacts.lock.json`, add downstream RED version/vertical/help/missing/refusal cases, and convert retained split integration rows to the deterministic fail-closed bridge from T023 in `crates/maw-cli/tests/native_attach_view_stream_split.rs`, `crates/maw-cli/tests/native_interactive_plugins.rs`, `crates/maw-cli/tests/verb_help_flag_cli.rs`, and `crates/maw-cli/tests/plugin_missing_verb_cli.rs`; preserve `crates/maw-cli/tests/fixtures/epic56/split-dry-run.stdout` and require the paired external committed-byte compatibility run against this exact downstream SHA before merge.
- [ ] T088 [maw-rs] [US3] Rebind `bud --split` to the accepted Split owner in `crates/maw-cli/src/core_impl/buddy_workspace.rs` while preserving the frozen best-effort/silently-discarded split-result contract; add exact present/missing/refused rows in its existing test module and defer any propagation change to a separately approved issue.
- [ ] T089 [maw-rs] [US3] On a fresh post-T086 branch, atomically cut over split ownership in `crates/maw-cli/src/core_impl/plugin_known_verbs.rs`, `crates/maw-cli/src/core_impl/help_meta.rs`, and `crates/maw-cli/src/core_impl/completions.rs`, delete only `crates/maw-cli/src/core_impl/split.rs`, and pass artifact/host/missing/refusal plus wake/attach/full gate before merge.
- [ ] T090 [maw-rs] [US3] Record proof that `maw-split` policy/shared `maw-tmux` actions remain plus exact gates and hashes in `specs/001-lean-core-plugins/issue-ledger.md` and the split child issue.

---

## Phase 6: User Story 4 - Team, Companions, and Atomic Cutover (Priority: P1)

### External team slices

Every T093-T119 implementation slice also wires its named module and route through
`packages/team/src/lib.rs` in the same bounded PR, and its native fake-Host test reaches that
production router rather than a test-only `#[path]` module. No source-only orphan may satisfy CI.

- [ ] T091 [maw-plugins] [US4] Reconcile all 43 names and their default/alias argv grammars in `packages/team/contract.json` and `packages/team/tests/command_matrix.rs`, explicitly including `live-check` and `delete`; freeze the full intent/effective-capability/resource/include-exclude/action-budget matrix and denial cases before source, then freeze native liveness/up/apply behavior on tmux inventory failure and obtain explicit human approval before changing empty/missing/fresh-wake behavior to a fail-closed typed error.
- [ ] T092 [maw-plugins] [US4] Scaffold the in-place Rust guest as both `cdylib` and `rlib` in `packages/team/Cargo.toml`, `packages/team/Cargo.lock`, and `packages/team/src/lib.rs` using `crates/maw-guest-sdk`; keep list/ls green, use the injected fake Host in native tests, and prove no WASI filesystem/environment/clock access.
- [ ] T093 [maw-plugins] [US4] Port list/ls in `packages/team/src/read_summary.rs` through bounded tool-teams/team-vault/team-state reads with frozen ordering/output rows.
- [ ] T094 [maw-plugins] [US4] Port status in `packages/team/src/status.rs` through bounded tool-teams/team-vault/team-state reads with legacy/error rows.
- [ ] T095 [maw-plugins] [US4] Port lives/history in `packages/team/src/history.rs` through bounded team-vault list/read and non-authority selected displayPath, preserving missing/error/count output without pane access.
- [ ] T096 [maw-plugins] [US4] Port members/oracle-members in `packages/team/src/members.rs` through merged primary/legacy team-state registry reads, without pane access.
- [ ] T097 [maw-plugins] [US4] Port liveness/live-check in `packages/team/src/liveness.rs` with the bound implicit-team-charter selector plus `maw.tmux.pane_inventory.v1`; freeze exact session/window and shell-vs-live member classification, preserve stale/missing/adopted-gone parity, and keep host inventory failure as an error.
- [ ] T098 [maw-plugins] [US4] Port plan parsing/rendering in `packages/team/src/plan.rs` through the route/argument-role-bound document selector with no host mutation; use only intent-bound resolve-only `previewDisplayPath` rows for frozen would-create path rendering, which creates no parent/ref/authority.
- [ ] T099 [maw-plugins] [US4] Port generic preflight/session/tool checks in `packages/team/src/preflight.rs` through pane inventory, issued workspace/engine refs, worktree inspect marker/branch facts, and `maw.engine.health.v1` executable/config `commandStatus` plus reviewed display facts from explicit-or-implicit operator document receipts; no arbitrary worktree/config read is allowed.
- [ ] T100 [maw-plugins] [US4] Only after accepted T153 provider descriptor/bytes exist, port provider auth/trust/isolation preflight in `packages/team/src/preflight_provider.rs` using only descriptor-bound `maw.engine.health.v1`; missing/unknown health cannot substitute for frozen Codex preflight parity.
- [ ] T101 [maw-plugins] [US4] Port create/new in `packages/team/src/state_create.rs` with invocation `nowMillis` and `maw.state.authority_write.v1` host-materialized authority fields/final records; freeze vault-manifest-first then tool-config ordering, including truthful vault-only state/record outcome when the second write fails, rather than inventing all-plan atomicity. Add atomic 0600/unknown-field fixtures and bounded non-authority created-manifest `displayPath` output.
- [ ] T102 [maw-plugins] [US4] Port load in `packages/team/src/state_load.rs` through the route/argument-role-bound document selector with invocation `nowMillis`, legacy preservation, and typed semantic `maw.state.authority_write.v1` tool-teams/team-vault writes; preserve exactly the frozen tool-config and vault-manifest collision guards plus native overwrite behavior for orphan inboxes, preserve tool-config -> member-inboxes -> vault-manifest ordering, return truthful state/record partial outcomes, and render only non-authority operation `displayPath` values.
- [ ] T103 [maw-plugins] [US4] Port task/tasks/add in `packages/team/src/state_tasks.rs` with invocation `nowMillis`; bind exact operator subject/description roles to `ContentRef` and persist them only through `writeBoundContent`. Preserve native counter-first then task-write ordering: a task-write failure leaves the host-derived counter consumed/gapped and reports the truthful partial result, rather than inventing all-or-none atomicity. Add content substitution/replay and counter/task partial-order fixtures.
- [ ] T104 [maw-plugins] [US4] Port assign/done in `packages/team/src/state_assign.rs` with exact argv-bound task-status/assignee transitions; preserve immutable host-materialized subject/description `ContentRef` bytes and reject status/assignee cross-task substitution or replay.
- [ ] T105 [maw-plugins] [US4] Port state-backed send/msg in `packages/team/src/messaging_send.rs` using invocation `nowMillis`, exact operator-payload `ContentRef`, and named-root `writeBoundContent`, not pane submission; freeze against the confirmed/ambiguous delivery semantics from closed #951 and merged #960 at the planning base.
- [ ] T106 [maw-plugins] [US4] Port broadcast and inbox as separate intents in `packages/team/src/messaging_inbox.rs`: broadcast alone gets invocation `nowMillis`, exact `ContentRef`, and host-materialized append/order behavior; inbox is read/list-only with no ContentRef, write action, or mutation budget. Freeze both against the confirmed/ambiguous delivery semantics from closed #951 and merged #960 at the planning base.
- [ ] T107 [maw-plugins] [US4] Port enter in `packages/team/src/submit.rs` using one-shot enterOnly `PanePayloadRef` plus `maw.workflow.pane_submit.v1` and the landed #960 confirmed/ambiguous contract.
- [ ] T108 [maw-plugins] [US4] Port send-enter in `packages/team/src/submit_enter.rs` with one-shot exact joined operator-argument `PanePayloadRef`, preflight-all, ambiguous refusal, and the landed #960 confirmed/ambiguous contract.
- [ ] T109 [maw-plugins] [US4] Port adopt/release in `packages/team/src/ownership.rs` as separate local/remote branches: local adopt requires one exact live session/window pane plus an issued validated WorkspaceRef or persist-only explicit `WorkspaceValueRef` and EngineRef, including host-side selected-pane classification when `--engine` is absent; remote adopt consumes an exact `PeerTrustRef`, not public status JSON. Adopt uses `maw.state.authority_write.v1 recordPeerDecision/upsertMemberFromRefs` so native code revalidates and materializes the new charter/tool-teams authority projection/final records; release is charter-only and updates the provenance record without tool-config/pane/lifecycle cleanup. Unknown/untrusted/ambiguous/stale/cross-peer lookup or partial state/record commit fails truthfully before later authority use.
- [ ] T110 [maw-plugins] [US4] Port reassign in `packages/team/src/reassign.rs` using the bound implicit charter selector, issued existing-member/workspace/engine refs, and one-shot `IssueRef`; the wrapped issue result plus manifest template yields the exact lifecycle prompt `ContentRef`. Before mutation, derive a removal-generation-bound `GitRefRef`/plannedWorkspaceRef for the exact fresh collision-safe destination; then preserve pre-done archive -> finish old member -> revalidate/create planned workspace -> launch ordering, update state/final record through typed semantic authority mutation, and report truthfully if finish succeeds but create/launch fails. Use no raw git/gh/content authority or credentials; freeze numbered-collision/race/partial rows.
- [ ] T111 [maw-plugins] [US4] In separate bounded slices, port member `remove` in `packages/team/src/ownership_remove.rs` through bound charter read, archive-before-done, issued existing-member finish, then charter-only semantic deletion/provenance update; preserve native tool config and every confirmed partial rather than adding cleanup. Port whole-team `delete`/`rm` in `packages/team/src/state_delete.rs` through named-root `archive_then_remove` plus authority-record retirement; use invocation `nowMillis`, preserve the exact alias split, and render truthful partial outcomes.
- [ ] T112 [maw-plugins] [US4] Port prune/gc in `packages/team/src/gc.rs` through invocation `nowMillis` and named-root `archive_then_remove` with stale-state fixtures.
- [ ] T113 [maw-plugins] [US4] Port spawn/spawn-from in `packages/team/src/lifecycle_spawn.rs` using route/argument-role-bound documents, Team `--parent`/`--parent-session-id`/`--session-id` refs, issued planned-member/bound-workspace-or-nativeDefaultResolution/engine refs plus engine-command receipts, and exact operator/default-template or final-record-existing spawn-file `ContentRef` values; writeBoundContent overwrites an explicit prompt or creates the default only when missing, while the lifecycle launch remains prompt-free. Use invocation `nowMillis`, planLaunch's host-rendered non-authority `displayCommand`, typed semantic authority writes/final records for tool-teams/team-vault, and lifecycle. Preserve direct/no-cwd and digest-bound member fallback as native resolution with no repo override. Preserve the frozen governance/target/name prechecks, then native team-create-before-sequential-member-validation/mutation order; a late invalid member reports already-confirmed state truthfully rather than silently gaining all-plan atomicity.
- [ ] T114 [maw-plugins] [US4] Port up/apply in `packages/team/src/lifecycle_up.rs` through explicit/implicit charters plus issued member/workspace/engine refs (including legacy member `model` fallback), engine-command receipts, typed semantic state/authority updates, and lifecycle; `worktree:false` uses `nativeDefaultResolution` with no repo override. For `up --gather`, freeze the pre-action live roster in the issued `LayoutPaneSetRef`, then run layout after the lifecycle loop only for those initially-live panes: newly resumed/woken/missing/dead members are excluded. Port bring separately in `packages/team/src/lifecycle_bring.rs` through durable-record-validated merged team-state/tool-teams membership reads, registry-member-relative inspected workspaces, frozen default EngineRefs, and issued owned-member refs; preserve native no-charter behavior plus the currently ignored parsed `--split`/`--gather` quirk.
- [ ] T115 [maw-plugins] [US4] Port resume using current-session/`nowMillis` context, final-record-validated named-root archived-manifest `/memberEngineCommands/<same member>` receipts, issued owned-member/nativeDefaultResolution/engine refs, and final-record-bound existing prompt or exact default spawn-file `ContentRef` created only when missing; lifecycle remains prompt-free. Use planLaunch's host-rendered non-authority `displayCommand`, one-shot `claimLeadFromCurrentSession`, typed semantic tool-teams/team-vault spawn writes, and lifecycle; omit repo overrides exactly as native resume does. Preserve native lead-claim mutation first; then validate all launch receipts before the first member state/lifecycle mutation and report claimed-state-on-later-manifest-error plus per-member partials truthfully. Preserve selected-manifest `displayPath` errors.
- [ ] T116 [maw-plugins] [US4] Port down in `packages/team/src/lifecycle_down.rs` through the bound implicit charter selector with issued owned-member refs, pre-done named-root archiveCopy for each member, host-owned keep-session guard, archive-before-lifecycle ordering, and truthful partial outcomes.
- [ ] T117 [maw-plugins] [US4] Port shutdown in `packages/team/src/lifecycle_shutdown.rs` with invocation `nowMillis`, issued owned-member refs, hash-covered shutdown-request template `ContentRef`/`writeBoundContent`, durable named-root archiveCopy before mailbox requests, bounded wait-reinventory, optional force terminate, mergeCopy, receipt-authorized conditional remove, and truthful partial outcomes.
- [ ] T118 [maw-plugins] [US4] Port invite parser/render in `packages/team/src/invite.rs` using invocation `nowMillis` and the typed public peer descriptor; invite authorization/state fields are never guest-authored or a trust mutation.
- [ ] T119 [maw-plugins] [US4] Integrate invite with one-shot `maw.consent.request.v1` in `packages/team/src/invite_consent.rs`; consume the exact public-peer/`ConsentDecisionRef` through `maw.state.authority_write.v1 recordPeerDecision` so native code revalidates/materializes authoritative invite state, and reject stale/cross-peer/replay/fabricated status. Reuse invocation `nowMillis`; preserve local-pending-before-POST ordering, unreachable exit 1 with local-mirror request id only, and successful pending request-id/pair-code/expiry/peer display with exit 2 while pending/denied confer no later member authority.

### External companions and acceptance

- [ ] T120 [maw-plugins] [US4] Implement/test/pin-source gather in `packages/gather/Cargo.toml`, `packages/gather/Cargo.lock`, `packages/gather/src/lib.rs`, `packages/gather/tests/contract.rs`, and `packages/gather/plugin.json` using a bound implicit/single-team `PaneInventoryScopeRef` plus exact `LayoutPaneSetRef`/intent/action budget only; build `packages/gather/plugin.wasm` but do not finalize the combined artifact set.
- [ ] T121 [maw-plugins] [US4] Implement/test/pin-source scatter in `packages/scatter/Cargo.toml`, `packages/scatter/Cargo.lock`, `packages/scatter/src/lib.rs`, `packages/scatter/tests/contract.rs`, and `packages/scatter/plugin.json` using a bound implicit/single-team `PaneInventoryScopeRef` plus exact `LayoutPaneSetRef`/intent/action budget only; build `packages/scatter/plugin.wasm`.
- [ ] T122 [maw-plugins] [US4] Create a compiling fake-Host/no-WASI scaffold in `packages/swarm/Cargo.toml`, `packages/swarm/Cargo.lock`, and `packages/swarm/src/lib.rs`, then implement parser/model/security in `packages/swarm/src/parse.rs` and `packages/swarm/tests/contract.rs`; the manifest exposes `cdylib`/`rlib`, custom agent executables use only route/argument-role-issued `EngineRef` values, the no-arg three-Claude default uses only a host-issued frozen-built-in ref, and `--parent`/`--parent-session-id`/`--session-id` use only issued session-environment refs.
- [ ] T123 [maw-plugins] [US4] Implement swarm plan/render in `packages/swarm/src/lib.rs` through `EngineRef`/`SessionEnvironmentRef`-only `maw.workflow.batch_launch.v1` and typed semantic tool-teams authority writes; preserve native `createdAt:0`, and freeze/obtain explicit approval before changing native plain-write mode/ordering to atomic private state.
- [ ] T124 [maw-plugins] [US4] Create a compiling fake-Host/no-WASI `cdylib`/`rlib` scaffold in `packages/artifacts/Cargo.toml`, `packages/artifacts/Cargo.lock`, and `packages/artifacts/src/lib.rs`, then implement artifacts/alias parser/model in `packages/artifacts/src/parse.rs` and `packages/artifacts/tests/contract.rs`, including canonical `artifacts` and alias `artifact`.
- [ ] T125 [maw-plugins] [US4] Implement artifact list/get/render in `packages/artifacts/src/lib.rs` through the named artifact-store root, using its bounded non-authority selected `displayPath` for frozen text/JSON `dir` output.
- [ ] T126 [maw-plugins] [US4] Only after T153 accepts the staged Codex provider, finalize/verify the complete production router and intent matrix in `packages/team/src/lib.rs`, retire stale `packages/team/src/plugin.ts` plus `packages/team/plugin.source.json`, remove the exact T022 legacy-team policy exemption, then build/pin/register complete bytes in `packages/team/plugin.json`, `packages/gather/plugin.json`, `packages/scatter/plugin.json`, `packages/swarm/plugin.json`, `packages/artifacts/plugin.json`, and route-aware `registry.json`; prove exhaustive #963-manifest policy scanning and require two clean canonical T018 builds of each of the five artifacts to match its committed pin. Pass T020/T021/T022/T072, directly invoke the full 43-name team plus every companion/alias/intent-denial matrix through exact-host compatibility against the frozen maw-rs SHA using only an isolated preseeded/sealed fake authority domain, pass full external CI, merge to `main`, and record accepted source/artifact/run SHAs before downstream worktrees exist.

### Downstream decoupling, companions, and team cutover

- [ ] T127 [maw-rs] [US4] Move `maw ls` team annotation behind a generic reader in `crates/maw-cli/src/core_impl/session_list_plan.rs`.
- [ ] T128 [maw-rs] [US4] Move invite request-id/PIN helpers behind consent utilities in `crates/maw-cli/src/core_impl/oracle_recruit.rs`.
- [ ] T129 [maw-rs] [US4] In bounded preparatory slices, edit `crates/maw-cli/src/core_impl/wave.rs`, `crates/maw-cli/src/core_impl/plugin_workflow_host.rs`, and `crates/maw-cli/src/core_impl/plugin_workflow_authority.rs` to replace every soon-deleted team type/helper with generic named-state, lifecycle, pane inventory/observation/submit, worktree, and durable authority-record adapters; native Wave writes/updates final records after the Team authority epoch and stays native until Codex cutover.
- [ ] T130 [maw-rs] [US4] On a fresh post-T126 branch, pin the accepted Team/companion commit and every artifact hash in `crates/maw-cli/tests/fixtures/external-plugin-artifacts.lock.json`, add committed-artifact/missing/refusal tests in `crates/maw-cli/tests/lean_core_team_plugins.rs`, and convert all retained suites to the deterministic fail-closed T023 bridge in `crates/maw-cli/tests/native_team_delete_prune.rs`, `native_team_down_teardown.rs`, `native_team_enter.rs`, `native_team_load_isolated_root.rs`, `native_team_messaging.rs`, `native_team_reassign.rs`, `native_team_remove.rs`, `native_team_resume.rs`, `native_team_shutdown.rs`, `native_team_spawn.rs`, `native_team_start_load.rs`, `native_team_status_dryrun.rs`, `native_team_task_ops.rs`, `native_team_up_apply_bring.rs`, `native_swarm_plugin.rs`, and `native_artifacts_plugin.rs`; paths are under `crates/maw-cli/tests/`, existing goldens remain, bounded conversion PRs precede cutover, and the exact downstream SHA must pass the paired external committed-byte compatibility matrix before merge.
- [ ] T131 [maw-rs] [US4] Atomically cut over gather ownership by removing only its `DISPATCH_329` entry in `crates/maw-cli/src/core_impl/team_gather_scatter.rs`, updating `crates/maw-cli/src/core_impl/plugin_known_verbs.rs`, `crates/maw-cli/src/core_impl/help_meta.rs`, and `crates/maw-cli/src/core_impl/completions.rs`, and passing artifact/missing/refusal plus wake/attach/full gate before merge.
- [ ] T132 [maw-rs] [US4] Atomically cut over scatter ownership by removing its remaining `DISPATCH_329` entry in `crates/maw-cli/src/core_impl/team_gather_scatter.rs`, updating `crates/maw-cli/src/core_impl/plugin_known_verbs.rs`, `crates/maw-cli/src/core_impl/help_meta.rs`, and `crates/maw-cli/src/core_impl/completions.rs`, and passing artifact/missing/refusal plus wake/attach/full gate before merge.
- [ ] T133 [maw-rs] [US4] Delete inert `crates/maw-cli/src/core_impl/team_gather_scatter.rs` in a deletion-only gated PR.
- [ ] T134 [maw-rs] [US4] In a bounded precursor slice, make every native Swarm state mutation in `crates/maw-cli/src/core_impl/swarm.rs` participate in the cross-process domain lock/generation protocol implemented by `crates/maw-cli/src/core_impl/plugin_workflow_authority.rs`. In the later atomic cutover, activate the retained first-plugin-entry migration hook in `crates/maw-cli/src/core_impl/dispatcher.rs`, prove incompatible writers quiescent, snapshot/revalidate/seal still-native Swarm state, refuse any orphan/race without plugin mutation, remove `DISPATCH_117`, and update `plugin_known_verbs.rs`, `help_meta.rs`, and `completions.rs`; pass concurrent-writer/unsealed/forged-record, artifact/missing/refusal, wake/attach, and full gates.
- [ ] T135 [maw-rs] [US4] Atomically cut over artifacts/`artifact` ownership by removing both `DISPATCH_71` entries in `crates/maw-cli/src/core_impl/artifacts.rs`, updating `crates/maw-cli/src/core_impl/plugin_known_verbs.rs`, `crates/maw-cli/src/core_impl/help_meta.rs`, and `crates/maw-cli/src/core_impl/completions.rs`, and passing canonical/alias artifact/missing/refusal plus wake/attach/full gate before merge.
- [ ] T136 [umbrella] [maw-rs] [US4] Track two exact deletion-only PRs for inert `crates/maw-cli/src/core_impl/swarm.rs` and `crates/maw-cli/src/core_impl/artifacts.rs`; each PR names its mechanical >250 exception when required and runs the exact gate.
- [ ] T137 [maw-rs] [US4] Record gather/scatter/swarm/artifacts/`artifact` accepted hashes, Swarm precursor-lock and per-machine epoch/schema evidence, cutover gates, wake/attach evidence, and deletion SHAs in `specs/001-lean-core-plugins/issue-ledger.md`.
- [ ] T138 [maw-rs] [US4] In bounded precursor slices, make every inventoried native Team authority mutation in `crates/maw-cli/src/core_impl/team_core.rs`, `team_tasks.rs`, `team_messaging.rs`, `team_adopt.rs`, `team_invite.rs`, `team_spawn.rs`, `team_resume.rs`, `team_up_apply.rs`, `team_reassign.rs`, `team_down.rs`, `team_remove.rs`, `team_delete.rs`, and `team_shutdown.rs` participate in the cross-process Team lock/generation protocol. In the later atomic switch, activate the retained first-plugin-entry scanner in `crates/maw-cli/src/core_impl/dispatcher.rs`/`plugin_workflow_authority.rs`, prove incompatible writers quiescent, snapshot/revalidate every legacy projection/full archived command and seal the Team epoch, then remove `DISPATCH_240` in `team_enter.rs`, neutralize unreachable `team_run_command`/router edges in `team_core.rs`, and update `plugin_known_verbs.rs`, `help_meta.rs`, and `completions.rs`. Pass concurrent-writer/post-epoch forged-no-record/orphan-reimport, committed-artifact 43-name/missing/refusal, wake/attach, quick, and full gates.
- [ ] T139 [umbrella] [maw-rs] [US4] After T127-T129 and T138 prove no external consumer remains and the native router is neutralized, track one compilable grouped deletion-only PR for the flat-include team dependency component: `crates/maw-cli/src/core_impl/team_enter.rs`, `crates/maw-cli/src/core_impl/team_messaging.rs`, `crates/maw-cli/src/core_impl/team_shutdown.rs`, `crates/maw-cli/src/core_impl/team_remove.rs`, `crates/maw-cli/src/core_impl/team_delete.rs`, `crates/maw-cli/src/core_impl/team_reassign.rs`, `crates/maw-cli/src/core_impl/team_down.rs`, `crates/maw-cli/src/core_impl/team_tasks.rs`, `crates/maw-cli/src/core_impl/team_up_apply.rs`, `crates/maw-cli/src/core_impl/team_spawn.rs`, `crates/maw-cli/src/core_impl/team_resume.rs`, `crates/maw-cli/src/core_impl/team_up_helpers.rs`, `crates/maw-cli/src/core_impl/team_adopt.rs`, `crates/maw-cli/src/core_impl/team_invite.rs`, `crates/maw-cli/src/core_impl/team_preflight_checks.rs`, and `crates/maw-cli/src/core_impl/team_core.rs`; disclose the mechanical >250 deletion exception, prove the grouped tree compiles/gates, and record its SHA, Team epoch evidence, 43-name matrix, and zero-reachable-native-team evidence in `specs/001-lean-core-plugins/issue-ledger.md`.

---

## Phase 7: User Story 5 - Codex Provider and Workflows (Priority: P2)

Every T142-T152 implementation slice also wires its named CLI module or provider export through
`packages/codex/src/lib.rs` in the same bounded PR, and its native fake-Host test reaches that
production router/export rather than a test-only `#[path]` module. No source-only orphan may satisfy
CI; T153 verifies the complete three-route plus provider-export graph.

- [ ] T140 [maw-rs] [US5] Complete vendor-policy/generic classification in `specs/001-lean-core-plugins/inventories/codex.md`, record `packages/codex` as sole owner of `codex`, `more`, and `wave`, freeze the full Wave default/status/start/heal/dispatch/teardown plus `down` alias matrix, freeze native more/wave pane-read failures that currently become empty/idle success, and obtain explicit human approval before replacing them with typed fail-closed errors.
- [ ] T141 [maw-plugins] [US5] Create `packages/codex/Cargo.toml` with `cdylib`/`rlib`, `packages/codex/Cargo.lock`, and scaffold route/intent-aware `packages/codex/src/lib.rs`; implement accounts parser/model rows in `packages/codex/src/accounts_model.rs` plus `packages/codex/tests/accounts.rs` using opaque occupancy facts and non-authority pid/displayHome fields required by frozen JSON output.
- [ ] T142 [maw-plugins] [US5] Implement accounts table rendering in `packages/codex/src/accounts_table.rs` with exact native parity.
- [ ] T143 [maw-plugins] [US5] Implement accounts JSON/free/slots behavior in `packages/codex/src/accounts.rs` with no raw secret/process output.
- [ ] T144 [maw-plugins] [US5] Add/implement more plan/discovery in `packages/codex/src/more_discover.rs` and `packages/codex/tests/more.rs`.
- [ ] T145 [maw-plugins] [US5] Implement more spawn/worktree marker behavior in `packages/codex/src/more_spawn.rs` through host-issued Codex `EngineRef` and non-mutating planned-workspace -> create -> existing-workspace flow in `maw.worktree.manage.v1`.
- [ ] T146 [maw-plugins] [US5] Implement more status in `packages/codex/src/more_status.rs` with pane-issued cwdRefs, typed worktree branch/marker, and pane title/index/active facts; displayed cwd strings are never worktree authority, and inventory failure is fail-closed only after T140 approval.
- [ ] T147 [maw-plugins] [US5] Implement more update/boot in `packages/codex/src/more.rs` through provider maintenance plus launch-only lifecycle-member/workspace/engine refs.
- [ ] T148 [maw-plugins] [US5] Add/implement wave start/heal in `packages/codex/src/wave_start.rs` and `packages/codex/tests/wave.rs` through planned-workspace create, issued lifecycle-member/workspace/engine refs, typed semantic wave-state write/final durable record with host-issued repo identity, and manifest-declared pane observations. Preserve native ordering/partials: create each missing worktree sequentially, write state only after all creates, then ensure session/up/heal; an early worktree failure leaves earlier worktrees but no state, while a later lifecycle failure leaves state and worktrees. A matched update prompt submits one enterOnly `PanePayloadRef` through the state-owned issued-member/pane path with full preflight, truthful ambiguous outcome, and no retry. No captured text crosses, and observation failure is fail-closed only after T140 approval.
- [ ] T149 [maw-plugins] [US5] Implement Wave's no-argument default as status and implement explicit status/dispatch in `packages/codex/src/wave_status.rs` through named state, host-revalidated repo identity/mission-store ref, exact operator/template mission `ContentRef`, and `writeBoundContent`; only the confirmed exact mission write issues the state-owned mission-pointer `PanePayloadRef`. Preserve write-before-submit, non-authority displayPath, truthful ambiguous submit, legacy-state revalidation, scoped pane inventory/observation, and post-T140 fail-closed pane errors.
- [ ] T150 [maw-plugins] [US5] Implement Wave teardown and its `down` alias identically in `packages/codex/src/wave_teardown.rs` through final durable-record-bound wave-state `ownedExisting lifecycleMemberRef` values whose workspace/branch/engine are derived natively by typed lifecycle/worktree operations; preserve named wave-state bytes and retain/update its final durable record so `status` and repeated `teardown|down` preserve native behavior. Freeze repeat-teardown/status-after-teardown rows and preserve non-authority archive-destination output.
- [ ] T151 [maw-plugins] [US5] Add provider launch/resume/profile/maintenance RED cases in `packages/codex/tests/provider.rs` against the exact vendored host ABI fixture; cover provider-default versus operator-complete composition, empty args/env in the latter, and proof the provider cannot see/drop/duplicate prompt or override native configured-command/resume precedence.
- [ ] T152 [maw-plugins] [US5] Implement the dedicated provider export in `packages/codex/src/provider.rs` using `crates/maw-guest-sdk`; provider code cannot call lifecycle/provider host functions.
- [ ] T153 [maw-plugins] [US5] Finalize the three-route CLI/provider-export integration in `packages/codex/src/lib.rs`, add the exact multi-route/provider descriptor plus per-route/subcommand intent/resource/action-budget tables to `packages/codex/plugin.json`, build/pin `packages/codex/plugin.wasm`, update route-aware `registry.json`, pass T020/T021/T022/T072 including two clean canonical T018 builds that match committed WASM, directly invoke committed bytes for all three routes/intent-denial matrix plus the dedicated plan-only provider export and re-entry denials through exact-host compatibility against the frozen maw-rs SHA using only an isolated preseeded/sealed fake Wave/Codex authority domain, pass full external CI, merge to `main`, and record accepted source/artifact/run SHAs.
- [ ] T154 [maw-rs] [US5] Only after T153, create a fresh alpha branch and monotonically advance the accepted Codex commit/artifact row in `crates/maw-cli/tests/fixtures/external-plugin-artifacts.lock.json`; add provider-present/missing/refused, canonical codex/more/wave ownership, plan-only import denial, no-reentry, native configured-command/resume/prompt composition, exactly-one-final-cwd-plan-call, and exactly-one existing dispatcher request/failure audit tests in `crates/maw-cli/tests/engine_provider_contract.rs` plus `crates/maw-cli/tests/lean_core_codex_plugins.rs`; prove there is no other wake-phase/workflow-filesystem/worktree/tmux mutation, convert `crates/maw-cli/tests/native_more_codex.rs` to the deterministic fail-closed T023 bridge before cutover, and require the paired external committed-byte/provider matrix against this exact downstream SHA before merge.
- [ ] T155 [maw-rs] [US5] In bounded pure-refactor/test slices followed by one atomic behavior switch, wire `crates/maw-cli/src/core_impl/dispatcher.rs`, `crates/maw-cli/src/core_impl/wake_engine_command.rs`, `crates/maw-cli/src/core_impl/wake.rs`, `crates/maw-cli/src/core_impl/workon.rs`, and `crates/maw-cli/src/core_impl/plugin_workflow_host.rs` to resolve a read-only exact target object-id/branch directory-config snapshot and final prospective cwd without mutation, invoke/cache the accepted provider plan exactly once before `wake_record_phase`, and hold a repo/ref CAS or lock from final revalidation through exact-object worktree add so a mutable branch cannot move at the create boundary. Make the old Codex fallback unreachable and preserve exactly one existing dispatcher request/failure audit while producing zero wake-phase/state/workflow-filesystem/worktree/tmux mutation beyond that append on plan failure; inject a ref move at create, existing-branch differing config, and snapshot race RED, and pass provider/non-Codex/unrelated-plugin wake/attach/full gates in the switch PR.
- [ ] T156 [maw-rs] [US5] In a bounded precursor slice, make every native Wave authority-state mutation in `crates/maw-cli/src/core_impl/wave.rs` participate in the cross-process Wave/Codex lock/generation protocol. In the later atomic switch, activate the retained first-plugin-entry scanner in `crates/maw-cli/src/core_impl/dispatcher.rs`/`plugin_workflow_authority.rs`, prove incompatible writers quiescent, snapshot/revalidate/seal still-native Wave state and refuse any orphan/race, then remove codex/more/wave dispatcher entries from `codex_accounts.rs`, `more.rs`, and `wave.rs`, neutralize the unreachable More router edge, and activate ownership in `plugin_known_verbs.rs`, `help_meta.rs`, and `completions.rs`; pass concurrent-writer/unsealed/forged-record, artifact/provider/missing/refusal, wake/attach, and full gates.
- [ ] T157 [maw-rs] [US5] Remove inert `crates/maw-cli/src/core_impl/codex_accounts.rs` in one deletion-only PR with explicit mechanical >250 justification and exact gate.
- [ ] T158 [umbrella] [maw-rs] [US5] Track one compilable grouped deletion-only PR for the flat-include More dependency component `crates/maw-cli/src/core_impl/more.rs`, `crates/maw-cli/src/core_impl/more_discover.rs`, `crates/maw-cli/src/core_impl/more_spawn.rs`, and `crates/maw-cli/src/core_impl/more_status.rs`, with an explicit mechanical >250 deletion exception and exact gate.
- [ ] T159 [maw-rs] [US5] Remove inert `crates/maw-cli/src/core_impl/wave.rs` after accepted artifact parity and source proof.
- [ ] T160 [maw-rs] [US5] Remove or neutralize the inventoried residual Codex product policy in `crates/maw-cli/src/core_impl/workon.rs`, `crates/maw-cli/src/core_impl/census.rs`, `crates/maw-cli/src/core_impl/worktree_finish.rs`, `crates/maw-cli/src/core_impl/wake_pane_launch.rs`, and `crates/maw-tmux/src/core_impl/action_resolution_parts/agent_pane_heuristic.rs`; retain a literal/provider observation only when `specs/001-lean-core-plugins/inventories/codex.md` classifies it as vendor-neutral fail-closed compatibility and the child records parity evidence.
- [ ] T161 [maw-rs] [US5] Record provider present/missing/refused/reentry/security matrices, Wave/Codex precursor-lock and per-machine epoch/schema evidence, provider-present Codex wake parity, missing-Codex pre-mutation failure, non-Codex/unrelated-plugin wake/attach invariance, exact gates, and accepted artifact/diff hashes in `specs/001-lean-core-plugins/issue-ledger.md`.

---

## Phase 8: User Story 6 - Ownership Diagnostics and Convergence (Priority: P2)

- [ ] T162 [P] [maw-rs] [US6] Add canonical/alias ownership convergence tests in `crates/maw-cli/tests/lean_core_ownership.rs` covering `artifact` and every extracted route; parse `specs/001-lean-core-plugins/issue-ledger.md` and assert convergence with dispatcher, known external ownership, help, completions, plugin list, and `crates/maw-cli/src/core_impl/doctor.rs` so every canonical/alias/package row has exactly one reachable owner.
- [ ] T163 [maw-rs] [US6] Update `docs/guides/extracting-a-verb.md` and `docs/guides/adding-a-plugin-artifact.md` with paired-repo artifact-first, staged shadows, multi-route flags, injected host ops, and provider rules.
- [ ] T164 [P] [maw-rs] [US6] Add source-boundary assertions in `crates/maw-cli/tests/lean_core_native_boundary.rs` for zero reachable team/bring/split/Codex product owners and retained wake/attach owners.
- [ ] T165 [maw-rs] [US6] Run Spec Kit analysis over the complete feature directory; resolve every CRITICAL/HIGH finding and record the report in `specs/001-lean-core-plugins/analysis.md`.
- [ ] T166 [maw-rs] [US6] Update `specs/001-lean-core-plugins/issue-ledger.md` and issue #963 with every child, merged SHA, artifact hash, gate, and remaining/closed ownership row.

---

## Phase 9: Final Verification and Release

- [ ] T167 [maw-plugins] Run full CI/pin checks, two clean byte-identical rebuilds matching every committed scoped `plugin.wasm`, `scripts/check-capability-policy.ts`, and the exact-host compatibility workflow against the final maw-rs candidate on the exact combined artifact tree; publish immutable toolchain/build/run/source/artifact evidence.
- [ ] T168 [maw-rs] On the exact unchanged combined alpha candidate, run unpiped/non-PTY `scripts/gate.sh full` with an isolated NVMe gate target and append its log hash to the immutable Child-25 external evidence record and issue #963; do not edit the candidate tree.
- [ ] T169 [maw-rs] On that same unchanged candidate, run `specs/001-lean-core-plugins/quickstart.md` plus installed-artifact smokes for bring/split/team/companions/Codex and missing/refused cases; append result hashes to the immutable Child-25 external evidence record and issue #963, not the candidate tree.
- [ ] T170 [maw-rs] Obtain independent frozen-byte security/behavior review for both repositories and append exact diff/artifact SHA-256 values to the immutable Child-25 external evidence record and issue #963 without changing either reviewed tree.
- [ ] T171 [maw-rs] Before the CalVer alpha tag, install the exact unchanged T167-T170 candidate as a canary and independently run native wake/attach/hey/peek/serve (including the accepted God origin) plus installed/missing/refused extracted-plugin and red-guard smokes. The God WebSocket canary MUST use a browser-equivalent client to assert the installed `maw --version` commit, negotiated `maw.ws.v1`, and `Sec-WebSocket-Accept`, never status-line-only success; record candidate tree, registry, artifact pins, and those assertions in Child-25 evidence. Only then follow `docs/guides/release-and-calver.md` to compute, cut, and independently verify the CalVer alpha; append release evidence and manually close alpha-merged children in issue #963. Any later in-tree ledger backfill is a separately labeled docs-only audit PR and is not evidence for the released candidate.

---

## Dependencies & Execution Order

- Phase 1 creates issue/ADR authority and the one-time docs/scaffold exception.
- T007-T017 land route/result contracts; T018-T024 then provide the minimal capability-free Rust guest ABI/SDK/CI, route-aware registry, policy, committed-artifact harness, and ownership diagnostics.
- T025-T069 land all typed host/runtime primitives; T070 freezes their complete ABI and T071-T072 extend the SDK/CI before any typed guest consumes them.
- Phase 3 wake/attach baseline is green before any downstream cutover.
- Bring depends only on T007-T024 and the native baseline, so it remains the first artifact/cutover MVP; split additionally waits for T033-T034 and T070-T072.
- Team read/generic preflight depends on pane, roots/context, provider health, and the accepted SDK;
  Codex-specific preflight and final Team acceptance additionally wait for the accepted staged T153
  provider descriptor/bytes. Team enter/send-enter and Wave's state-owned update-prompt Enter/
  mission pointer use pane submit, while mailbox messaging uses ContentRef-bound named state.
- Companions depend on typed layout, batch launch, named artifact/tool roots, and worktree/provider primitives as applicable.
- No Team artifact-test/cutover/native-removal branch (T130+) starts before accepted T126 artifacts;
  generic preparatory decoupling may land earlier. T126/T138 depend on accepted
  Codex provider bytes for frozen provider-specific preflight, but do not depend on downstream Codex
  wake wiring/cutover/deletion; T129 decouples native wave first.
- No Codex downstream branch starts before accepted T153 combined artifact. Provider wiring T155 must merge green before atomic command cutover T156.
- Convergence and release depend on every selected story and all named deletion trains.

## Parallel Opportunities

- T003-T005 inventories can run in parallel.
- Independent host RED/implementation/adapter slices may run in isolated worktrees, but ABI fixture T070 waits for all of them.
- Bring source and acceptance may complete after the base SDK/CI/policy while unrelated typed-host foundations continue.
- Team read/state and Codex source tests may progress in isolated external branches; `maw-plugins/main` serializes accepted artifacts.
- Every downstream cutover begins only from the post-acceptance alpha named by its child issue.

## Implementation Strategy

1. Land governance and native wake/attach baseline.
2. Land route/result plus the minimal capability-free SDK/CI and complete bring as the artifact-first MVP.
3. Land injected typed primitives, freeze the complete ABI, extend the SDK/CI, then complete split.
4. Build generic Team/companions and Codex source in parallel; accept the staged Codex/provider
   artifact first, then finish provider-specific Team preflight and accept Team/companion bytes.
5. Start only fresh post-acceptance downstream branches; Team and Codex cutovers proceed through
   their explicit dependencies, with provider wiring green before atomic Codex command cutover.
6. Delete inert native source only in exact named mechanical PRs.
7. Converge ownership/security, run exact combined gates, and release after independent review.

## Format Validation

All 171 tasks use checkbox syntax and sequential `T001`-`T171`. Every task names its repository;
story tasks carry `[US#]`. Executable implementation tasks name exact paths. Wildcards are absent from
implementation tasks; `[umbrella]` is reserved for exact, named deletion-only PR trains.

## Requirement Coverage

- FR-001..FR-002 -> T073-T076, T155, T161, T164, T171.
- FR-003..FR-004 -> T007-T024, T077-T083; FR-005..FR-008 -> T007-T024, T033-T034, T070-T072, T084-T090.
- FR-009..FR-016 -> T025-T072, T091-T139.
- FR-017..FR-021 -> T052-T072, T140-T161.
- FR-022..FR-025 -> T007-T015, T023-T024, T162-T166.
- FR-026..FR-028 -> every artifact/cutover gate plus T167-T170.
- FR-029 -> T138-T164; FR-030 -> T006 and T166.
- SC-001..SC-010 -> T073-T076, T083, T090, T137, T139, T161, and T162-T171.
